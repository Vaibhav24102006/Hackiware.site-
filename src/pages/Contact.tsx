// Temporary shim: forward to the new ContactPage component
// Keep a stable re-export here so other files that import `Contact` keep working.
// The real, polished contact page lives in `ContactPage.tsx`.
import ContactPage from './ContactPage';

export default ContactPage;


